import 'package:flutter/material.dart';

// void main() {
//   runApp(MyApp());
// }

// Below code is the same as the above
void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text('Testing push'),
    );
  }
}
